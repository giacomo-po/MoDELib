close all
clear all
clc

s0=rand(6,1);
e0=rand(6,1);
C=rand(6,6);
C=C'*C;
eP=rand(6,1);
%alpha=[0 0 0 0 0 0];
switch strategy
    case 1
        
end
alpha=[0 0 1e10 0 0 0];
%alpha=[1 1 1 1 1 1]*1e10;
A=diag(alpha);
A1=diag(1./(1+alpha));
A2=diag(alpha./(1+alpha));
e=inv(C)*A1*s0+inv(C)*A2*C*(e0-eP)+eP;
s=A1*s0+A2*C*(e0-eP);

[e e0 s s0]
