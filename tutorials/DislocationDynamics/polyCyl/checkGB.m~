clear all
close all
clc

figure(1)
clf
hold on
N=42;
for f=1:N
G=load(['gb' num2str(f) '.txt']);
R=load(['rb' num2str(f) '.txt']);
for r=1:size(R,1)
plot3(R)
end
patch(G(:,1),G(:,2),G(:,3),'g')
end
axis equal